{% extends 'base.html.twig' %}

{% block title %}Hello FigureController!{% endblock %}

{% block body %}
    {# block qui affiche les données de l'entité figure #}
    <div>
        <h1>{{ figure.name }}</h1>
        <p>{{ figure.description }}</p>
        <p>{{ figure.createdAt | date('d/m/Y') }}</p>
    </div>
{% endblock %}
